--- #Koality: 1337
setup:
- databases:
  - postgres:
    - name: koality
      username: koality
      password: lt3
- scripts:
  # install go
  - curl -L https://go.googlecode.com/files/go1.2.linux-amd64.tar.gz -s -o - | tar -C /usr/local -xzf -
  - export GOROOT=/home/lt3/koality-v1/code/back
  - export PATH=$PATH:/usr/local/go/bin:$GOROOT/bin
factories:
  - koality test factory:
    script:
    - |
      cd code/back/src/koality
      ls -d */ | while read testDirectory; do
        echo - go test koality/$testDirectory...:
      done
test:
  machines: 1
